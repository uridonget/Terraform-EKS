# mail-server-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: mail-server-service
  namespace: neves
spec:
  selector:
    app: mail-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP