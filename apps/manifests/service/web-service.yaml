apiVersion: v1
kind: Service
metadata:
  name: my-sample-app-service
  namespace: default
spec:
  selector:
    app: my-sample-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort